# Branding assets

These are **original** (non-Flash) assets used for app icons and splash screens.

- `icon.png` – source icon (1024×1024)
- `splash.png` – source splash (2732×2732)

To regenerate and re-apply platform assets:

```bash
python3 scripts/generate_branding_assets.py
```

