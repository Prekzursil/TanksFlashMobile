# Ruffle (generated)

This folder is populated by `npm run sync:ruffle` and contains the self-hosted Ruffle runtime:

- `ruffle.js`
- `*.wasm`
- `core.ruffle.*.js`

These files are copied from `node_modules/@ruffle-rs/ruffle/` and are not committed to git.

